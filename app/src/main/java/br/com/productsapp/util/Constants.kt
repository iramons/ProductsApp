package br.com.productsapp.util

// path calls API
object APIPaths {
    private const val sandbox = "sandbox"
    const val products = "${sandbox}/products"
}

object Preferences {
    const val DB_WORKER_THREAD_NAME             = "dbWorkerThread"
    const val DATE_FORMAT                       = "yyyy-MM-dd'T'HH:mm:ss"
}

